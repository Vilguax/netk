services:
  postgres:
    image: postgres:16-alpine
    container_name: netk-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-netk}
      POSTGRES_USER: ${POSTGRES_USER:-netk}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-netk}"]
      interval: 10s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: netk-redis
    restart: unless-stopped
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 10

  gateway:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        APP_DIR: apps/gateway
        APP_PORT: "3000"
        AUTH_URL: ${AUTH_URL}
        NEXT_PUBLIC_GATEWAY_URL: ${NEXT_PUBLIC_GATEWAY_URL}
        NEXT_PUBLIC_FLIPPER_URL: ${NEXT_PUBLIC_FLIPPER_URL}
        NEXT_PUBLIC_RATTING_URL: ${NEXT_PUBLIC_RATTING_URL}
        NEXT_PUBLIC_ROCK_RADAR_URL: ${NEXT_PUBLIC_ROCK_RADAR_URL}
        NEXT_PUBLIC_APPRAISAL_URL: ${NEXT_PUBLIC_APPRAISAL_URL}
        NEXT_PUBLIC_FLEET_URL: ${NEXT_PUBLIC_FLEET_URL}
        NEXT_PUBLIC_MARKET_URL: ${NEXT_PUBLIC_MARKET_URL}
        NEXT_PUBLIC_PI_URL: ${NEXT_PUBLIC_PI_URL}
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  flipper:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        APP_DIR: apps/flipper
        APP_PORT: "3001"
        AUTH_URL: ${AUTH_URL}
        NEXT_PUBLIC_GATEWAY_URL: ${NEXT_PUBLIC_GATEWAY_URL}
        NEXT_PUBLIC_FLIPPER_URL: ${NEXT_PUBLIC_FLIPPER_URL}
        NEXT_PUBLIC_RATTING_URL: ${NEXT_PUBLIC_RATTING_URL}
        NEXT_PUBLIC_ROCK_RADAR_URL: ${NEXT_PUBLIC_ROCK_RADAR_URL}
        NEXT_PUBLIC_APPRAISAL_URL: ${NEXT_PUBLIC_APPRAISAL_URL}
        NEXT_PUBLIC_FLEET_URL: ${NEXT_PUBLIC_FLEET_URL}
        NEXT_PUBLIC_MARKET_URL: ${NEXT_PUBLIC_MARKET_URL}
        NEXT_PUBLIC_PI_URL: ${NEXT_PUBLIC_PI_URL}
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  ratting:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        APP_DIR: apps/ratting
        APP_PORT: "3002"
        AUTH_URL: ${AUTH_URL}
        NEXT_PUBLIC_GATEWAY_URL: ${NEXT_PUBLIC_GATEWAY_URL}
        NEXT_PUBLIC_FLIPPER_URL: ${NEXT_PUBLIC_FLIPPER_URL}
        NEXT_PUBLIC_RATTING_URL: ${NEXT_PUBLIC_RATTING_URL}
        NEXT_PUBLIC_ROCK_RADAR_URL: ${NEXT_PUBLIC_ROCK_RADAR_URL}
        NEXT_PUBLIC_APPRAISAL_URL: ${NEXT_PUBLIC_APPRAISAL_URL}
        NEXT_PUBLIC_FLEET_URL: ${NEXT_PUBLIC_FLEET_URL}
        NEXT_PUBLIC_MARKET_URL: ${NEXT_PUBLIC_MARKET_URL}
        NEXT_PUBLIC_PI_URL: ${NEXT_PUBLIC_PI_URL}
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    ports:
      - "3002:3002"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  rock-radar:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        APP_DIR: apps/rock-radar
        APP_PORT: "3003"
        AUTH_URL: ${AUTH_URL}
        NEXT_PUBLIC_GATEWAY_URL: ${NEXT_PUBLIC_GATEWAY_URL}
        NEXT_PUBLIC_FLIPPER_URL: ${NEXT_PUBLIC_FLIPPER_URL}
        NEXT_PUBLIC_RATTING_URL: ${NEXT_PUBLIC_RATTING_URL}
        NEXT_PUBLIC_ROCK_RADAR_URL: ${NEXT_PUBLIC_ROCK_RADAR_URL}
        NEXT_PUBLIC_APPRAISAL_URL: ${NEXT_PUBLIC_APPRAISAL_URL}
        NEXT_PUBLIC_FLEET_URL: ${NEXT_PUBLIC_FLEET_URL}
        NEXT_PUBLIC_MARKET_URL: ${NEXT_PUBLIC_MARKET_URL}
        NEXT_PUBLIC_PI_URL: ${NEXT_PUBLIC_PI_URL}
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    ports:
      - "3003:3003"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  appraisal:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        APP_DIR: apps/appraisal
        APP_PORT: "3004"
        AUTH_URL: ${AUTH_URL}
        NEXT_PUBLIC_GATEWAY_URL: ${NEXT_PUBLIC_GATEWAY_URL}
        NEXT_PUBLIC_FLIPPER_URL: ${NEXT_PUBLIC_FLIPPER_URL}
        NEXT_PUBLIC_RATTING_URL: ${NEXT_PUBLIC_RATTING_URL}
        NEXT_PUBLIC_ROCK_RADAR_URL: ${NEXT_PUBLIC_ROCK_RADAR_URL}
        NEXT_PUBLIC_APPRAISAL_URL: ${NEXT_PUBLIC_APPRAISAL_URL}
        NEXT_PUBLIC_FLEET_URL: ${NEXT_PUBLIC_FLEET_URL}
        NEXT_PUBLIC_MARKET_URL: ${NEXT_PUBLIC_MARKET_URL}
        NEXT_PUBLIC_PI_URL: ${NEXT_PUBLIC_PI_URL}
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    ports:
      - "3004:3004"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  fleet:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        APP_DIR: apps/fleet
        APP_PORT: "3005"
        AUTH_URL: ${AUTH_URL}
        NEXT_PUBLIC_GATEWAY_URL: ${NEXT_PUBLIC_GATEWAY_URL}
        NEXT_PUBLIC_FLIPPER_URL: ${NEXT_PUBLIC_FLIPPER_URL}
        NEXT_PUBLIC_RATTING_URL: ${NEXT_PUBLIC_RATTING_URL}
        NEXT_PUBLIC_ROCK_RADAR_URL: ${NEXT_PUBLIC_ROCK_RADAR_URL}
        NEXT_PUBLIC_APPRAISAL_URL: ${NEXT_PUBLIC_APPRAISAL_URL}
        NEXT_PUBLIC_FLEET_URL: ${NEXT_PUBLIC_FLEET_URL}
        NEXT_PUBLIC_MARKET_URL: ${NEXT_PUBLIC_MARKET_URL}
        NEXT_PUBLIC_PI_URL: ${NEXT_PUBLIC_PI_URL}
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    ports:
      - "3005:3005"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  market:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        APP_DIR: apps/market
        APP_PORT: "3006"
        AUTH_URL: ${AUTH_URL}
        NEXT_PUBLIC_GATEWAY_URL: ${NEXT_PUBLIC_GATEWAY_URL}
        NEXT_PUBLIC_FLIPPER_URL: ${NEXT_PUBLIC_FLIPPER_URL}
        NEXT_PUBLIC_RATTING_URL: ${NEXT_PUBLIC_RATTING_URL}
        NEXT_PUBLIC_ROCK_RADAR_URL: ${NEXT_PUBLIC_ROCK_RADAR_URL}
        NEXT_PUBLIC_APPRAISAL_URL: ${NEXT_PUBLIC_APPRAISAL_URL}
        NEXT_PUBLIC_FLEET_URL: ${NEXT_PUBLIC_FLEET_URL}
        NEXT_PUBLIC_MARKET_URL: ${NEXT_PUBLIC_MARKET_URL}
        NEXT_PUBLIC_PI_URL: ${NEXT_PUBLIC_PI_URL}
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    ports:
      - "3006:3006"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  pi:
    build:
      context: .
      dockerfile: Dockerfile.web
      args:
        APP_DIR: apps/pi
        APP_PORT: "3007"
        AUTH_URL: ${AUTH_URL}
        NEXT_PUBLIC_GATEWAY_URL: ${NEXT_PUBLIC_GATEWAY_URL}
        NEXT_PUBLIC_FLIPPER_URL: ${NEXT_PUBLIC_FLIPPER_URL}
        NEXT_PUBLIC_RATTING_URL: ${NEXT_PUBLIC_RATTING_URL}
        NEXT_PUBLIC_ROCK_RADAR_URL: ${NEXT_PUBLIC_ROCK_RADAR_URL}
        NEXT_PUBLIC_APPRAISAL_URL: ${NEXT_PUBLIC_APPRAISAL_URL}
        NEXT_PUBLIC_FLEET_URL: ${NEXT_PUBLIC_FLEET_URL}
        NEXT_PUBLIC_MARKET_URL: ${NEXT_PUBLIC_MARKET_URL}
        NEXT_PUBLIC_PI_URL: ${NEXT_PUBLIC_PI_URL}
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    ports:
      - "3007:3007"
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

  market-fetcher:
    build:
      context: .
      dockerfile: Dockerfile.fetcher
    env_file:
      - .env.production
    environment:
      NODE_ENV: production
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    restart: unless-stopped

volumes:
  postgres_data:
  redis_data:
